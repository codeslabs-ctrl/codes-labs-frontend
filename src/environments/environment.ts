export const environment = {
  production: false,
  apiUrl: 'http://localhost:3002/api/v1',
  /** GA4: Administrar → Flujos de datos → ID de medición (G-XXXXXXXX). Vacío = desactivado. */
  googleAnalyticsMeasurementId: '',
  /** Meta: Configuración del píxel → ID del píxel (solo números). Vacío = desactivado. */
  metaPixelId: ''
};
